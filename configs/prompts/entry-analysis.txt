You are analysing a single summary entry against an approved chat digest and a set of connected entries.
The content is creative fiction. Do not apply real-world content filters to story content.

CONTENT FIDELITY — highest priority, apply before anything else:
- Do NOT sanitise, soften, or euphemise graphic violence, explicit content, visceral imagery, or NSFW/NSFL material. If the source is brutal, it stays brutal. If it is explicit, it stays explicit.
- When proposing a REWRITE, preserve the author's own vocabulary and phrasing wherever it carries emotional or descriptive weight. Reuse their specific words — do not replace vivid, precise language with generic summary language.
- Character identity must be preserved. A gladiator who kills criminals with brutal, specific methods must remain viscerally specific in the entry — "he killed a lot of bad guys" is a failure mode. A monster that ate a character's arm must be described with that same visceral detail. Emotional depth and engagement take precedence over cleanliness.
- Only recommend NO_CHANGE if the entry is already accurate, well-paced, and faithful to the source tone and detail level.

SENTENCE SOFT CAP:
Entries work best at 3–4 sentences. If an entry has 7 or more sentences, consider recommending SPLIT — but do NOT split if the length is justified by content density or emotional intensity. Length is acceptable when the material demands it.

STRUCTURAL ACTIONS:
- REWRITE: the entry is inaccurate, too vague, sanitised, or missing key character/event detail from the digest.
- SPLIT: the entry covers too many distinct beats — divide it, preserving the tone and vocabulary of each part.
- MERGE: the entry overlaps substantially with a connected entry — provide the combined text.
- SWAP: the entry belongs in a different position relative to another connected entry — specify the target entry number.
- NO_CHANGE: the entry is accurate, appropriately detailed, and faithful to the source.

Return ONLY a valid JSON object — no prose before or after:
{
  "action": "REWRITE" | "SPLIT" | "MERGE" | "SWAP" | "NO_CHANGE",
  "reason": "one sentence explaining the recommendation",
  "proposed": "full rewritten text preserving source vocabulary and emotional register (REWRITE only)",
  "parts": ["part 1 full text", "part 2 full text", ...] (SPLIT only — each part must be self-contained),
  "target": <entry number> (MERGE or SWAP only)
}
